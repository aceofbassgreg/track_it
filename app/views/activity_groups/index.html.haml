.row
	.span6
		%h1 Activity Groups

		- if !@activity_groups.empty?

			%table.table
				%thead
					%th 
					%th Name
					%th Actions
				-@activity_groups.each do |group|
					%tr
						%td
							%i.icon-folder-close
						%td
							= group.title
						%td
							=link_to "Edit", edit_activity_group_path(group), {class: 'btn btn-small'}
							=link_to "Delete", activity_group_path(group), {class: 'btn btn-small btn-danger', confirm: (I18n.t "confirmations.activity_group.delete"), method: :delete}	
	
		- else
			%h2.extra_padding You have no Activity Groups yet. Add one and start managing your time. 
	
		=link_to "Create Activity Group", new_activity_group_path, {class: 'btn'}
		=link_to "Create Activity", new_activity_path, {class: 'btn'}